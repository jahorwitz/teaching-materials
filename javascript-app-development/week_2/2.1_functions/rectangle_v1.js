// Let's create a program to calculate the area of a rectangle

// First, let's define our length and width
let length = 5;
let width = 10;

// Now, let's calulate the area of our rectangle

let area = length * width;

// Finally, print the result to the console

console.log(`Area = ${area}`);