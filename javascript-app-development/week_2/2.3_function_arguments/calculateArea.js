module.exports = function() {
    let length = 5;
    let width = 10;

    // Now, let's calulate the area of our rectangle

    let area = length * width;

    // Instead of printing the result here, we can just return the value itself

    return area;
};