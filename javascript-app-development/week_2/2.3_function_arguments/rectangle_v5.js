// What if we want to make the calculateArea function calculate the area of ANY rectangle?

// We need to pass ARGUMENTS to the function

const calculateArea = require('./calculateArea');

const area = calculateArea();

console.log(`Area = ${area}`);